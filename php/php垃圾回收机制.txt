1：引用计数器：
    每个php变量存在一个叫zval的变量容器中；该容器中包含变量的值，类型；
    还有标识变量是否属于引用集合:is_ref,以及标识指向该zval容器的变量个数：refcount；

    其中refcount在变量被声明时，其值就会设置为1；之后每当把变量赋值给另一个变量的时候将增加引用次数；即refcount将会加1；

    而unset变量的时候，其中该zval容器中的refcount值将会减一